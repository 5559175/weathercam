# Weathercam

Gemini-coded script and index.html to capture still 4K image from a Reolink CX810 security camera, and push to GitHub (to serve via Pages to permit easy sharing elsewhere).

Script can be run on a cron schedule to update the image periodically.
