# Weathercam

Gemini-coded script and index.html to capture image from a Reolink CX810 security camera, and push to a github repo (to serve via Pages). Script can be run on a cron schedule to update the image periodically.
